export interface Course {
    id: string;
    courseName: string;
    management: string;
    place: string;
    weekDay: string;
    courseTime: string;
    startDate: string;
    endDate: string;
    selected: boolean;
}